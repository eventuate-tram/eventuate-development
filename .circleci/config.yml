version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
    working_directory: ~/eventuate-development
    steps:
      - checkout
      - run:
          command: ./build-and-publish.sh
